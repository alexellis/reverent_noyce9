name: workflow

# Generated by alexellis/actuated-batch at: 2023-12-20 15:40:01.800507 +0000 GMT m=+0.001903793
# Job requested by alex

on:
  pull_request:
    branches:
      - '*'
  push:
    branches:
      - master
      - main

jobs:
  workflow:
    name: reverent_noyce9
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Run the job
        run: |
          chmod +x ./job.sh
          ./job.sh
